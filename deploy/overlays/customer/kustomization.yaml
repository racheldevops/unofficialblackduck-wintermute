apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: blackduck-wintermute

resources:
  - ../../base
  - namespace.yaml

images:
  - name: blackduck-wintermute
    newName: registry.invalid/customer/blackduck-wintermute
    newTag: replace-me

patches:
  - path: cronjob-patch.yaml

configMapGenerator:
  - name: blackduck-wintermute-jira-config
    behavior: replace
    files:
      - jira-rollup-config.json

generatorOptions:
  disableNameSuffixHash: true
