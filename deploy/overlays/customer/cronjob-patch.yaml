apiVersion: batch/v1
kind: CronJob
metadata:
  name: blackduck-jira-pipeline
spec:
  schedule: "0 2 * * *"
  timeZone: "Etc/UTC"
  suspend: true
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 21600
      template:
        spec:
          imagePullSecrets:
            - name: blackduck-harness-registry
          containers:
            - name: jira-pipeline
              imagePullPolicy: IfNotPresent
              args:
                - --dry-run
                - --strict
                - --config
                - /etc/blackduck-harness/jira-rollup-config.json
              env:
                - name: BLACKDUCK_URL
                  valueFrom:
                    secretKeyRef:
                      name: blackduck-harness-credentials
                      key: BLACKDUCK_URL
                - name: BLACKDUCK_API_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: blackduck-harness-credentials
                      key: BLACKDUCK_API_TOKEN
                - name: JIRA_URL
                  valueFrom:
                    secretKeyRef:
                      name: blackduck-harness-credentials
                      key: JIRA_URL
                - name: JIRA_USER
                  valueFrom:
                    secretKeyRef:
                      name: blackduck-harness-credentials
                      key: JIRA_USER
                - name: JIRA_API_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: blackduck-harness-credentials
                      key: JIRA_API_TOKEN
              resources:
                requests:
                  cpu: 250m
                  memory: 512Mi
                limits:
                  cpu: "2"
                  memory: 2Gi