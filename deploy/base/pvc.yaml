apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blackduck-wintermute-data
  labels:
    app.kubernetes.io/name: blackduck-wintermute
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
